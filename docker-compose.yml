version: '3.8'

services:
  # Consul service for service discovery
  consul:
    image: consul:1.15
    ports:
      - "8500:8500"
      - "8600:8600/udp"
    environment:
      - CONSUL_BIND_INTERFACE=eth0
      - CONSUL_CLIENT_INTERFACE=eth0
    restart: always
    healthcheck:
      test: ["CMD", "consul", "members", "-http-addr", "http://localhost:8500"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Nacos service for configuration center
  nacos:
    image: nacos/nacos-server:v2.2.3
    ports:
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    environment:
      - MODE=standalone
      - NACOS_AUTH_ENABLE=false
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8848/nacos/v1/console/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ZooKeeper service (Kafka dependency)
  zookeeper:
    image: bitnami/zookeeper:3.8
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    restart: always
    volumes:
      - zookeeper-data:/bitnami/zookeeper

  # Kafka service
  kafka:
    image: bitnami/kafka:3.3
    ports:
      - "9092:9092"
      - "9093:9093"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092,EXTERNAL://localhost:9093
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - ALLOW_PLAINTEXT_LISTENER=yes
      - KAFKA_CFG_NUM_PARTITIONS=3
      - KAFKA_CFG_DEFAULT_REPLICATION_FACTOR=1
    depends_on:
      - zookeeper
    restart: always
    volumes:
      - kafka-data:/bitnami/kafka

  # Redis cache service
  redis:
    image: redis:7.0-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: always
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # MySQL database service
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
    volumes:
      - mysql-data:/var/lib/mysql
      - mysql-config:/etc/mysql/conf.d
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 30s
      timeout: 10s
      retries: 3

  # MQTT broker for 5G communication
  mosquitto:
    image: eclipse-mosquitto:2.0
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    restart: always
    healthcheck:
      test: ["CMD", "mosquitto_sub", "-t", "health", "-h", "localhost", "-C", "1"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Istio components
  istio-pilot:
    image: istio/pilot:1.18.0
    ports:
      - "15010:15010"
      - "15011:15011"
      - "15012:15012"
    environment:
      - PILOT_TRACE_SAMPLING=100
      - ISTIO_META_HTTP10=1
    restart: always
    depends_on:
      - consul

  istio-proxy:
    image: istio/proxyv2:1.18.0
    ports:
      - "15000:15000"
      - "15001:15001"
      - "15006:15006"
    restart: always
    depends_on:
      - istio-pilot

  # User service
  user-service:
    build:
      context: ./apps/services/user-service
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - USER_SERVICE_PORT=3001
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=user-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Ride service
  ride-service:
    build:
      context: ./apps/services/ride-service
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=development
      - RIDE_SERVICE_PORT=3002
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=ride-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Ecommerce service
  ecommerce-service:
    build:
      context: ./apps/services/ecommerce-service
      dockerfile: Dockerfile
    ports:
      - "3003:3003"
    environment:
      - NODE_ENV=development
      - ECOMMERCE_SERVICE_PORT=3003
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=ecommerce-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3003/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Payment service
  payment-service:
    build:
      context: ./apps/services/payment-service
      dockerfile: Dockerfile
    ports:
      - "3004:3004"
    environment:
      - NODE_ENV=development
      - PAYMENT_SERVICE_PORT=3004
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=payment-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3004/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Order service
  order-service:
    build:
      context: ./apps/services/order-service
      dockerfile: Dockerfile
    ports:
      - "3005:3005"
    environment:
      - NODE_ENV=development
      - ORDER_SERVICE_PORT=3005
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=order-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3005/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Social service
  social-service:
    build:
      context: ./apps/services/social-service
      dockerfile: Dockerfile
    ports:
      - "3006:3006"
    environment:
      - NODE_ENV=development
      - SOCIAL_SERVICE_PORT=3006
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=social-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3006/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Portal service
  portal-service:
    build:
      context: ./apps/services/portal-service
      dockerfile: Dockerfile
    ports:
      - "3007:3007"
    environment:
      - NODE_ENV=development
      - PORTAL_SERVICE_PORT=3007
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=portal-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3007/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Common service
  common-service:
    build:
      context: ./apps/services/common-service
      dockerfile: Dockerfile
    ports:
      - "3008:3008"
    environment:
      - NODE_ENV=development
      - COMMON_SERVICE_PORT=3008
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=common-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3008/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Food service
  food-service:
    build:
      context: ./apps/services/food-service
      dockerfile: Dockerfile
    ports:
      - "3009:3009"
    environment:
      - NODE_ENV=development
      - FOOD_SERVICE_PORT=3009
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=food-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3009/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Errand service
  errand-service:
    build:
      context: ./apps/services/errand-service
      dockerfile: Dockerfile
    ports:
      - "3010:3010"
    environment:
      - NODE_ENV=development
      - ERRAND_SERVICE_PORT=3010
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=errand-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3010/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Transaction service
  transaction-service:
    build:
      context: ./apps/services/transaction-service
      dockerfile: Dockerfile
    ports:
      - "3011:3011"
    environment:
      - NODE_ENV=development
      - TRANSACTION_SERVICE_PORT=3011
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=transaction-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3011/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Blockchain service
  blockchain-service:
    build:
      context: ./apps/services/blockchain-service
      dockerfile: Dockerfile
    ports:
      - "3012:3012"
    environment:
      - NODE_ENV=development
      - BLOCKCHAIN_SERVICE_PORT=3012
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=blockchain-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
      - ETHEREUM_PROVIDER=https://mainnet.infura.io/v3/YOUR_INFURA_KEY
      - CONTRACT_ADDRESS=0xYourContractAddress
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3012/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 5G service
  5g-service:
    build:
      context: ./apps/services/5g-service
      dockerfile: Dockerfile
    ports:
      - "3013:3013"
    environment:
      - NODE_ENV=development
      - FIVE_G_SERVICE_PORT=3013
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=5g-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
      - MQTT_BROKER=mqtt://mosquitto:1883
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
      - mosquitto
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3013/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Edge service
  edge-service:
    build:
      context: ./apps/services/edge-service
      dockerfile: Dockerfile
    ports:
      - "3014:3014"
    environment:
      - NODE_ENV=development
      - EDGE_SERVICE_PORT=3014
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=edge-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
      - MQTT_BROKER=mqtt://mosquitto:1883
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
      - mosquitto
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3014/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Cache service
  cache-service:
    build:
      context: ./apps/services/cache-service
      dockerfile: Dockerfile
    ports:
      - "3015:3015"
    environment:
      - NODE_ENV=development
      - CACHE_SERVICE_PORT=3015
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=cache-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3015/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Smart traffic service
  smart-traffic:
    build:
      context: ./apps/services/smart-traffic
      dockerfile: Dockerfile
    ports:
      - "3016:3016"
    environment:
      - NODE_ENV=development
      - SMART_TRAFFIC_PORT=3016
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=smart-traffic
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3016/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Supply chain service
  supply-chain:
    build:
      context: ./apps/services/supply-chain
      dockerfile: Dockerfile
    ports:
      - "3017:3017"
    environment:
      - NODE_ENV=development
      - SUPPLY_CHAIN_PORT=3017
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=supply-chain
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3017/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Operation platform service
  operation-platform:
    build:
      context: ./apps/services/operation-platform
      dockerfile: Dockerfile
    ports:
      - "3018:3018"
    environment:
      - NODE_ENV=development
      - OPERATION_PLATFORM_PORT=3018
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=operation-platform
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3018/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Customer service system
  customer-service:
    build:
      context: ./apps/services/customer-service
      dockerfile: Dockerfile
    ports:
      - "3019:3019"
    environment:
      - NODE_ENV=development
      - CUSTOMER_SERVICE_PORT=3019
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=customer-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3019/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Security service
  security-service:
    build:
      context: ./apps/services/security-service
      dockerfile: Dockerfile
    ports:
      - "3020:3020"
    environment:
      - NODE_ENV=development
      - SECURITY_SERVICE_PORT=3020
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=security-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3020/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Developer platform service
  developer-platform:
    build:
      context: ./apps/services/developer-platform
      dockerfile: Dockerfile
    ports:
      - "3021:3021"
    environment:
      - NODE_ENV=development
      - DEVELOPER_PLATFORM_PORT=3021
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=developer-platform
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
      - JWT_SECRET=your-secret-key
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3021/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # i18n service
  i18n-service:
    build:
      context: ./apps/services/i18n-service
      dockerfile: Dockerfile
    ports:
      - "3022:3022"
    environment:
      - NODE_ENV=development
      - I18N_SERVICE_PORT=3022
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=i18n-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3022/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # AI service
  ai-service:
    build:
      context: ./apps/services/ai-service
      dockerfile: Dockerfile
    ports:
      - "3023:3023"
    environment:
      - NODE_ENV=development
      - AI_SERVICE_PORT=3023
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_HOST=kafka
      - KAFKA_PORT=9092
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=mixmlaal
      - MYSQL_USER=mixmlaal
      - MYSQL_PASSWORD=mixmlaal
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - SERVICE_HOST=ai-service
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
      - MONGO_URI=mongodb://mongo:27017/mixmlaal
    depends_on:
      - consul
      - nacos
      - redis
      - kafka
      - mysql
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3023/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # API gateway
  api-gateway:
    build:
      context: ./apps/api-gateway
      dockerfile: Dockerfile
    ports:
      - "80:3000"
      - "9091:9091"  # Prometheus metrics
    environment:
      - NODE_ENV=development
      - API_GATEWAY_PORT=3000
      - USER_SERVICE_URL=http://user-service:3001
      - RIDE_SERVICE_URL=http://ride-service:3002
      - ECOMMERCE_SERVICE_URL=http://ecommerce-service:3003
      - PAYMENT_SERVICE_URL=http://payment-service:3004
      - ORDER_SERVICE_URL=http://order-service:3005
      - SOCIAL_SERVICE_URL=http://social-service:3006
      - PORTAL_SERVICE_URL=http://portal-service:3007
      - FOOD_SERVICE_URL=http://food-service:3009
      - ERRAND_SERVICE_URL=http://errand-service:3010
      - COMMON_SERVICE_URL=http://common-service:3008
      - TRANSACTION_SERVICE_URL=http://transaction-service:3011
      - BLOCKCHAIN_SERVICE_URL=http://blockchain-service:3012
      - FIVE_G_SERVICE_URL=http://5g-service:3013
      - EDGE_SERVICE_URL=http://edge-service:3014
      - CACHE_SERVICE_URL=http://cache-service:3015
      - SMART_TRAFFIC_URL=http://smart-traffic:3016
      - SUPPLY_CHAIN_URL=http://supply-chain:3017
      - OPERATION_PLATFORM_URL=http://operation-platform:3018
      - CUSTOMER_SERVICE_URL=http://customer-service:3019
      - SECURITY_SERVICE_URL=http://security-service:3020
      - DEVELOPER_PLATFORM_URL=http://developer-platform:3021
      - I18N_SERVICE_URL=http://i18n-service:3022
      - AI_SERVICE_URL=http://ai-service:3023
      - CONSUL_HOST=consul
      - CONSUL_PORT=8500
      - NACOS_HOST=nacos
      - NACOS_PORT=8848
      - NACOS_NAMESPACE=mixmlaal
      - NACOS_GROUP=DEFAULT_GROUP
      - METRICS_PORT=9091
      - ISTIO_PROXY_HOST=istio-proxy
      - ISTIO_PROXY_PORT=15006
    depends_on:
      - consul
      - user-service
      - ride-service
      - ecommerce-service
      - payment-service
      - order-service
      - social-service
      - portal-service
      - food-service
      - errand-service
      - common-service
      - transaction-service
      - blockchain-service
      - 5g-service
      - edge-service
      - cache-service
      - smart-traffic
      - supply-chain
      - operation-platform
      - customer-service
      - security-service
      - developer-platform
      - i18n-service
      - ai-service
      - istio-proxy
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Prometheus service for monitoring
  prometheus:
    image: prom/prometheus:v2.45.0
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    restart: always
    depends_on:
      - api-gateway

  # Grafana service for visualization
  grafana:
    image: grafana/grafana:10.0.0
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource
    restart: always
    depends_on:
      - prometheus

  # Elasticsearch for log storage
  elasticsearch:
    image: elasticsearch:7.17.13
    container_name: elasticsearch
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
      - xpack.security.enabled=false
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\"\\|\"status\":\"yellow\"'"]
      interval: 30s
      timeout: 10s
      retries: 5

  # Logstash for log processing
  logstash:
    image: logstash:7.17.13
    container_name: logstash
    ports:
      - "5044:5044"
      - "9600:9600"
    volumes:
      - ./logstash/pipeline:/usr/share/logstash/pipeline:ro
      - ./logstash/config/logstash.yml:/usr/share/logstash/config/logstash.yml:ro
    environment:
      - "LS_JAVA_OPTS=-Xms256m -Xmx256m"
    restart: always
    depends_on:
      - elasticsearch

  # Kibana for log visualization
  kibana:
    image: kibana:7.17.13
    container_name: kibana
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - SERVER_NAME=mixmlaal-kibana
    restart: always
    depends_on:
      - elasticsearch

  # Jaeger for distributed tracing
  jaeger:
    image: jaegertracing/all-in-one:1.48
    container_name: jaeger
    ports:
      - "6831:6831/udp"
      - "16686:16686"
      - "14268:14268"
      - "14250:14250"
      - "9411:9411"
    environment:
      - SPAN_STORAGE_TYPE=elasticsearch
      - ES_SERVER_URLS=http://elasticsearch:9200
      - COLLECTOR_ZIPKIN_HOST_PORT=:9411
    restart: always
    depends_on:
      - elasticsearch

  # Alertmanager for alerting
  alertmanager:
    image: prom/alertmanager:v0.26.0
    container_name: alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager-data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    restart: always

  # Logging aggregator service
  logging-aggregator:
    build:
      context: ./apps/services/logging-aggregator
      dockerfile: Dockerfile
    ports:
      - "3024:3024"
    environment:
      - NODE_ENV=production
      - LOGGING_AGGREGATOR_PORT=3024
      - ELASTICSEARCH_HOST=http://elasticsearch:9200
      - FLUENTD_HOST=logstash
      - FLUENTD_PORT=5000
    restart: always
    depends_on:
      - elasticsearch
      - logstash

  # Tracing proxy service
  tracing-proxy:
    build:
      context: ./apps/services/tracing-proxy
      dockerfile: Dockerfile
    ports:
      - "3025:3025"
    environment:
      - NODE_ENV=production
      - TRACING_PROXY_PORT=3025
      - JAEGER_URL=http://jaeger:14268/api/traces
      - ELASTICSEARCH_HOST=http://elasticsearch:9200
    restart: always
    depends_on:
      - jaeger
      - elasticsearch

  # Metrics collector service
  metrics-collector:
    build:
      context: ./apps/services/metrics-collector
      dockerfile: Dockerfile
    ports:
      - "3026:3026"
    environment:
      - NODE_ENV=production
      - METRICS_COLLECTOR_PORT=3026
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    restart: always
    depends_on:
      - redis

volumes:
  zookeeper-data:
  kafka-data:
  redis-data:
  mysql-data:
  mysql-config:
  prometheus-data:
  grafana-data:
  elasticsearch-data:
  alertmanager-data: