# MIXMLAAL 系统架构设计说明书

| 属性 | 内容 |
|------|------|
| **文档版本** | v1.0 |
| **编制时间** | 2025-01-15 |
| **架构风格** | 微服务架构 |
| **适用阶段** | 设计与实施 |

---

## 一、整体架构概览

### 1.1 架构设计原则

| 原则 | 说明 | 实践 |
|------|------|------|
| 高内聚低耦合 | 相关功能聚合，模块间依赖最小化 | 服务拆分、接口抽象 |
| 弹性设计 | 考虑各种故障场景，具备容错能力 | 熔断、降级、限流 |
| 可扩展 | 支持水平扩展，应对业务增长 | 无状态服务、分布式缓存 |
| 可观测 | 完整的日志、监控、追踪体系 | ELK + Prometheus + Jaeger |
| 安全性 | 全面考虑安全风险 | 认证、授权、加密、审计 |
| 自动化 | 减少人工干预，提升效率 | CI/CD、自动化运维 |

### 1.2 系统分层架构

```
┌─────────────────────────────────────────────────────────┐
│                     用户接入层                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │  iOS    │  │Android │  │   H5    │  │小程序   │    │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘    │
│       └───────────┴───────────┴───────────┘           │
│                         │                               │
│                    CDN 加速                             │
└─────────────────────────┼───────────────────────────────┘
                          │
┌─────────────────────────┼───────────────────────────────┐
│                    网关接入层                            │
├─────────────────────────┼───────────────────────────────┤
│                         ▼                               │
│  ┌─────────────────────────────────────────────────┐    │
│  │              API Gateway / BFF                   │    │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐          │    │
│  │  │鉴权认证 │ │限流熔断 │ │路由转发 │          │    │
│  │  └─────────┘ └─────────┘ └─────────┘          │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────┼───────────────────────────────┘
                          │
┌─────────────────────────┼───────────────────────────────┐
│                     业务服务层                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │用户服务   │  │订单服务   │  │支付服务   │               │
│  │user-svc  │  │order-svc │  │payment-svc│              │
│  └──────────┘  └──────────┘  └──────────┘               │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │司机服务   │  │骑手服务   │  │商品服务   │               │
│  │driver-svc│  │rider-svc │  │product-svc│              │
│  └──────────┘  └──────────┘  └──────────┘               │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │位置服务   │  │消息服务   │  │AI服务    │               │
│  │location  │  │notify-svc│  │ai-service│              │
│  └──────────┘  └──────────┘  └──────────┘               │
│                                                         │
└─────────────────────────┼───────────────────────────────┘
                          │
┌─────────────────────────┼───────────────────────────────┐
│                      数据访问层                           │
├─────────────────────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐        │
│  │ MySQL  │  │ Redis  │  │ Kafka  │  │ OSS    │        │
│  └────────┘  └────────┘  └────────┘  └────────┘        │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────┼───────────────────────────────┐
│                      基础设施层                           │
├─────────────────────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐        │
│  │ K8s   │  │ Docker │  │Nginx   │  │ CI/CD  │        │
│  └────────┘  └────────┘  └────────┘  └────────┘        │
└─────────────────────────────────────────────────────────┘
```

### 1.3 流量架构图

```
                              用户请求
                                 │
                    ┌────────────┴────────────┐
                    ▼                         ▼
              国内用户                    海外用户
              (CDN China)                (CDN Global)
                    │                         │
                    └────────────┬────────────┘
                                 ▼
                         ┌──────────────┐
                         │   负载均衡器   │
                         │  (SLB/ALB)    │
                         └───────┬──────┘
                                 │
              ┌──────────────────┼──────────────────┐
              ▼                  ▼                  ▼
        ┌─────────┐        ┌─────────┐        ┌─────────┐
        │ Gateway │        │ Gateway │        │ Gateway │
        │ Node 1  │        │ Node 2  │        │ Node N  │
        └────┬────┘        └────┬────┘        └────┬────┘
             │                   │                   │
             └───────────────────┼───────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    ▼                         ▼
              BFF 层                      直接调用
         (业务聚合网关)                     微服务
```

---

## 二、微服务架构设计

### 2.1 服务拆分策略

#### 拆分原则

```
拆分依据：
├── 业务边界（高内聚）
│   ├── 每个服务对应一个完整的业务领域
│   └── 避免跨领域数据直接依赖
├── 变更频率（独立部署）
│   ├── 频繁变更的功能独立拆分
│   └── 稳定功能可以合并
├── 团队结构（康威定律）
│   ├── 每个服务由一个团队负责
│   └── 2 Pizza Team 原则
└── 性能隔离
    ├── 高性能要求的服务独立部署
    └── 避免资源争抢
```

#### 服务列表

| 服务名称 | 服务职责 | 团队归属 | 部署规格建议 |
|----------|----------|----------|--------------|
| gateway-service | API网关、路由、认证 | 平台组 | 4核8G × 3 |
| user-service | 用户管理、认证授权 | 用户组 | 2核4G × 2 |
| order-service | 订单创建、状态管理 | 交易组 | 4核8G × 4 |
| payment-service | 支付处理、清结算 | 支付组 | 2核4G × 3 |
| driver-service | 司机管理、调度 | 出行组 | 4核8G × 3 |
| rider-service | 骑手管理、调度 | 外卖组 | 4核8G × 3 |
| merchant-service | 商家管理 | 商家组 | 2核4G × 2 |
| product-service | 商品管理、SKU | 商城组 | 2核4G × 2 |
| location-service | 位置服务、轨迹 | 出行组 | 4核8G × 3 |
| notify-service | 消息推送、短信 | 平台组 | 2核4G × 2 |
| ai-service | AI功能、智能推荐 | AI组 | 8核16G × 2 |
| analytics-service | 数据分析、报表 | 数据组 | 4核8G × 2 |
| search-service | 全文搜索 | 平台组 | 4核8G × 2 |

### 2.2 服务间通信设计

#### 同步通信（HTTP/gRPC）

```
使用场景：
├── 用户登录 → 获取用户信息
├── 创建订单 → 校验用户、校验库存
├── 支付回调 → 更新订单状态
└── 查询订单 → 获取订单详情

通信规范：
┌────────────────────────────────────────┐
│            HTTP/gRPC 请求              │
├────────────────────────────────────────┤
│ Headers:                               │
│   X-Request-ID: uuid                  │
│   X-Trace-ID: uuid                    │
│   Authorization: Bearer token         │
│   X-From-Service: service-name       │
├────────────────────────────────────────┤
│ Body:                                  │
│   {
│     "method": "getUserInfo",          │
│     "params": { "userId": "xxx" },    │
│     "timestamp": 1234567890          │
│   }
└────────────────────────────────────────┘
```

#### 异步通信（Kafka）

```
使用场景：
├── 订单创建 → 触发多系统协同处理
│   ├── 扣减库存
│   ├── 发送通知
│   ├── 更新统计
│   └── 触发风控
├── 支付完成 → 通知各相关系统
│   ├── 订单服务更新状态
│   ├── 商家收到订单
│   ├── 骑手接单
│   └── 财务记账
└── 用户行为 → 事件采集
    ├── 点击事件
    ├── 浏览事件
    └── 搜索事件

Topic 设计：
┌─────────────────────────────────────────────────────────┐
│ Topic 命名规范: {domain}.{entity}.{action}             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  order.created        # 订单创建                        │
│  order.paid           # 订单支付                        │
│  order.completed      # 订单完成                        │
│  order.cancelled      # 订单取消                        │
│                                                         │
│  payment.succeeded    # 支付成功                        │
│  payment.failed       # 支付失败                        │
│  payment.refunded     # 退款成功                        │
│                                                         │
│  user.registered      # 用户注册                        │
│  user.authenticated   # 用户认证                        │
│                                                         │
│  driver.location      # 司机位置更新                    │
│  rider.location       # 骑手位置更新                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.3 服务治理

#### 熔断降级策略

```yaml
熔断器配置:
  # 全局默认配置
  default:
    slowRatio: 0.5          # 慢调用比例阈值
    slowTime: 3000          # 慢调用阈值(ms)
    minRequest: 10          # 最小请求数
    breakTime: 60000        # 熔断持续时间(ms)
    successRatio: 0.6       # 恢复成功率要求
  
  # 特定服务配置
  services:
    payment-service:
      slowTime: 5000        # 支付服务可接受更长延迟
      breakTime: 120000     # 熔断时间更长
      
    ai-service:
      slowTime: 10000       # AI服务延迟较高
      fallback: "ai-fallback-response"  # 降级响应

降级策略:
  - 本地缓存：当服务不可用时，返回缓存数据
  - 兜底数据：当核心功能不可用时，返回默认响应
  - 功能降级：关闭非核心功能，保证核心流程
```

#### 限流策略

```
限流维度：
├── 按用户限流
│   ├── 普通用户：100次/分钟
│   └── VIP用户：500次/分钟
├── 按IP限流
│   └── 1000次/分钟
└── 按接口限流
    ├── 下单接口：100次/分钟
    ├── 支付接口：50次/分钟
    └── 查询接口：1000次/分钟

限流算法：
┌────────────────────────────────────────┐
│              令牌桶算法                 │
├────────────────────────────────────────┤
│                                        │
│    ┌──────┐    令牌    ┌──────────┐   │
│    │Token │ ──────→ │  桶容量   │   │
│    │Bucket│    添加    │ maxTokens │   │
│    └──────┘           └──────────┘   │
│         ↑                              │
│         │                              │
│    请求消费 ←────────── 速率控制         │
│                                        │
│  优点：支持突发流量                     │
│  配置：rate=100, capacity=500          │
│                                        │
└────────────────────────────────────────┘
```

---

## 三、数据架构设计

### 3.1 数据库选型

| 数据库类型 | 使用场景 | 代表产品 | 容量规划 |
|----------|----------|----------|----------|
| 关系型数据库 | 核心业务数据 | MySQL | 10TB+ |
| 内存数据库 | 缓存、会话 | Redis | 100GB |
| 文档数据库 | 非结构化数据 | MongoDB | 5TB |
| 搜索引擎 | 全文检索 | Elasticsearch | 3TB |
| 时序数据库 | 监控数据 | InfluxDB | 2TB |
| 图数据库 | 社交关系 | Neo4j | 500GB |
| 对象存储 | 文件、图片 | OSS | 100TB+ |

### 3.2 数据分库分表策略

```
分库分表原则：
├── 按业务拆分
│   ├── 用户库（user_db）
│   ├── 订单库（order_db）
│   ├── 支付库（payment_db）
│   └── 商品库（product_db）
├── 按数据量拆分
│   └── 单表数据量超过 5000 万行时考虑分表
└── 分片键选择
    └── 订单表：按 user_id 分片
    └── 支付表：按 order_id 分片

数据分片架构：
┌─────────────────────────────────────────────────────────┐
│                    ShardingSphere                        │
│                    数据分片中间件                         │
└─────────────────────────────────────────────────────────┘
           │               │               │
           ▼               ▼               ▼
    ┌────────────┐  ┌────────────┐  ┌────────────┐
    │  Order DB  │  │  Order DB  │  │  Order DB  │
    │   Shard 0  │  │   Shard 1  │  │   Shard N  │
    │ (orders_0) │  │ (orders_1) │  │(orders_n)  │
    └────────────┘  └────────────┘  └────────────┘
```

### 3.3 缓存架构设计

```
多级缓存架构：

┌─────────────────────────────────────────────────────────┐
│ L1: 本地缓存 (Caffeine/Guava)                            │
│     ├── 命中率: 60%                                      │
│     ├── 延迟: < 1ms                                     │
│     └── 适用: 几乎不变的数据                              │
│         ├── 配置信息                                     │
│         ├── 枚举值                                       │
│         └── 热门商品信息                                  │
├─────────────────────────────────────────────────────────┤
│ L2: 分布式缓存 (Redis Cluster)                           │
│     ├── 命中率: 30%                                      │
│     ├── 延迟: 5-10ms                                    │
│     └── 适用: 热点数据                                   │
│         ├── 用户信息                                     │
│         ├── 商家信息                                     │
│         └── 订单缓存                                     │
├─────────────────────────────────────────────────────────┤
│ L3: 消息队列 (Kafka)                                    │
│     ├── 异步解耦                                         │
│     ├── 流量削峰                                         │
│     └── 适用: 非实时场景                                 │
│         ├── 数据同步                                     │
│         ├── 事件通知                                     │
│         └── 统计计算                                     │
└─────────────────────────────────────────────────────────┘
```

### 3.4 数据同步方案

```
MySQL 与 Redis 同步策略：

Cache Aside（旁路缓存）
┌────────────────────────────────────────┐
│                                        │
│  Read:
│  1. 读取 Redis
│  2. 未命中 → 读 MySQL
│  3. 写入 Redis
│                                        │
│  Write:
│  1. 写入 MySQL
│  2. 删除 Redis（延迟双删）
│                                        │
└────────────────────────────────────────┘

 Canal 增量同步：
 ┌────────┐    Binlog    ┌─────────┐    增量    ┌────────┐
 │ MySQL  │ ──────────→ │  Canal  │ ────────→ │ Redis  │
 └────────┘              └─────────┘            └────────┘
```

---

## 四、部署架构设计

### 4.1 容器化部署

```
Kubernetes 部署架构：

┌─────────────────────────────────────────────────────────┐
│                   Kubernetes Cluster                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Master Node (3)                    │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │Scheduler│  │API Server│  │Controller│      │   │
│  │  └──────────┘  └──────────┘  └──────────┘      │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Worker Node Pool A (业务服务)       │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐  │   │
│  │  │user-svc│ │order-  │ │payment │ │driver- │  │   │
│  │  │        │ │svc     │ │-svc    │ │svc     │  │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Worker Node Pool B (数据服务)       │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐  │   │
│  │  │ MySQL  │ │ Redis  │ │ Kafka  │ │ ES     │  │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.2 命名空间隔离

```yaml
命名空间设计:
  # 生产环境
  production:
    - namespace: mixla-prod
      services: 所有生产服务
      replicas: 3+
      
  # 预发布环境
  staging:
    - namespace: mixla-staging
      services: 待发布服务
      replicas: 1-2
      
  # 测试环境
  testing:
    - namespace: mixla-test
      services: 测试服务
      replicas: 1
      
  # 开发环境
  development:
    - namespace: mixla-dev
      services: 开发中服务
      replicas: 1
```

### 4.3 资源配额

```yaml
资源配额模板:

# 服务资源配额
service-resources:
  # 微服务（一般业务逻辑）
  microservice:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "2000m"
      memory: "2Gi"
      
  # 高内存服务
  high-memory:
    requests:
      cpu: "1000m"
      memory: "2Gi"
    limits:
      cpu: "4000m"
      memory: "8Gi"
      
  # 数据库
  database:
    requests:
      cpu: "2000m"
      memory: "4Gi"
    limits:
      cpu: "8000m"
      memory: "32Gi"
      
# HPA 自动扩缩容
hpa-config:
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
```

### 4.4 多环境部署

```
┌─────────────────────────────────────────────────────────┐
│                  多环境部署架构                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  开发环境 (Dev)
│  ├── 目的: 开发调试
│  ├── 配置: 本地/Docker Compose
│  └── 数据: 测试数据
│                                                         │
│  测试环境 (Test)
│  ├── 目的: 功能测试、集成测试
│  ├── 配置: K8s 单机集群
│  └── 数据: 脱敏生产数据
│                                                         │
│  预发布环境 (Staging)
│  ├── 目的: 预发布验证、性能测试
│  ├── 配置: K8s 多节点集群
│  └── 数据: 影子数据（隔离）
│                                                         │
│  生产环境 (Production)
│  ├── 目的: 正式服务
│  ├── 配置: K8s 高可用集群
│  └── 数据: 真实数据
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 五、关键技术决策

### 5.1 为什么选择微服务架构？

| 考量因素 | 单体架构 | 微服务架构 | 选择理由 |
|----------|----------|------------|----------|
| 业务复杂度 | 低 | 高 | 四大业务线需要独立演进 |
| 团队规模 | 小 | 大 | 多个团队需要并行开发 |
| 部署频率 | 低 | 高 | 需要快速迭代 |
| 技术多样性 | 受限 | 自由 | 不同服务可用不同技术 |
| 故障隔离 | 差 | 好 | 一服务故障不影响全局 |

### 5.2 为什么选择 Kubernetes？

| 考量因素 | 评估 |
|----------|------|
| 容器编排能力 | ★★★★★ 自动调度、自愈、扩缩容 |
| 生态成熟度 | ★★★★★ 社区活跃、周边工具完善 |
| 多云支持 | ★★★★☆ 天然支持多云部署 |
| 学习曲线 | ★★★☆☆ 有一定学习成本 |
| 运维复杂度 | ★★★☆☆ 需要专业运维团队 |

### 5.3 为什么选择 Kafka？

| 考量因素 | 评估 |
|----------|------|
| 消息可靠性 | ★★★★★ 持久化、多副本 |
| 吞吐量 | ★★★★★ 单机百万级 |
| 消息顺序性 | ★★★★☆ 分区内有序 |
| 生态集成 | ★★★★★ 与大数据生态深度集成 |
| 运维复杂度 | ★★★☆☆ 需要专业运维 |

### 5.4 技术选型决策矩阵

```
技术选型评估表（5分制）：

┌──────────────┬─────────┬─────────┬─────────┐
│   技术领域    │  方案A  │  方案B  │  方案C  │
├──────────────┼─────────┼─────────┼─────────┤
│   消息队列    │  Kafka  │ RabbitMQ│  Redis  │
├──────────────┼─────────┼─────────┼─────────┤
│   可靠性      │   5     │   4     │   3     │
│   吞吐量      │   5     │   3     │   4     │
│   生态        │   5     │   4     │   3     │
│   运维成本     │   3     │   4     │   5     │
│   学习成本     │   3     │   4     │   5     │
├──────────────┼─────────┼─────────┼─────────┤
│   综合得分     │  21     │  19     │  20     │
│   推荐方案     │   ✓     │         │         │
└──────────────┴─────────┴─────────┴─────────┘
```

---

## 六、安全架构

### 6.1 安全体系架构

```
┌─────────────────────────────────────────────────────────┐
│                     安全防护体系                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  网络层                                                  │
│  ├── VPC 隔离                                           │
│  ├── 安全组/防火墙                                       │
│  ├── DDoS 防护                                          │
│  └── WAF 应用防火墙                                      │
│                                                         │
│  应用层                                                  │
│  ├── 身份认证（OAuth2/JWT）                             │
│  ├── 权限控制（RBAC）                                   │
│  ├── 输入验证                                           │
│  └── XSS/SQL注入防护                                    │
│                                                         │
│  数据层                                                  │
│  ├── 传输加密（TLS 1.3）                                │
│  ├── 存储加密（AES-256）                                 │
│  ├── 密钥管理（KMS）                                    │
│  └── 敏感数据脱敏                                        │
│                                                         │
│  审计层                                                  │
│  ├── 操作日志                                            │
│  ├── 安全审计                                            │
│  └── 合规报告                                            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.2 认证授权设计

```
JWT Token 认证流程：

┌─────────────────────────────────────────────────────────┐
│                      认证流程                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  用户 ──→ 登录 ──→ 验证凭证 ──→ 生成Token ──→ 返回      │
│                          │                              │
│                          ▼                              │
│                    存储 RefreshToken                    │
│                    (Redis/DB)                          │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │                  Token 结构                       │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ Access Token:                                   │   │
│  │   - 有效期: 15分钟                               │   │
│  │   - 内容: userId, roles, permissions           │   │
│  │                                                  │   │
│  │ Refresh Token:                                  │   │
│  │   - 有效期: 7天                                  │   │
│  │   - 内容: userId, deviceId                      │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 七、运维保障体系

### 7.1 监控体系

```
监控架构：

┌─────────────────────────────────────────────────────────┐
│                      监控体系                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  Metrics    │  │   Logs      │  │   Traces    │    │
│  │ (Prometheus)│  │  (ELK)      │  │  (Jaeger)   │    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘    │
│         │                │                │            │
│         └────────────────┼────────────────┘            │
│                          ▼                              │
│                   ┌─────────────┐                       │
│                   │   Grafana   │                       │
│                   │   可视化    │                       │
│                   └─────────────┘                       │
│                          │                              │
│                          ▼                              │
│                   ┌─────────────┐                       │
│                   │   AlertManager│                      │
│                   │   告警通知   │                       │
│                   └─────────────┘                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 7.2 告警策略

```yaml
告警级别定义:
  P0 - 紧急:
    - 定义: 服务完全不可用
    - 响应时间: 5分钟
    - 通知方式: 电话 + 短信 + 钉钉
    - 示例: API 不可用、数据库宕机
    
  P1 - 严重:
    - 定义: 核心功能受损
    - 响应时间: 15分钟
    - 通知方式: 短信 + 钉钉
    - 示例: 支付失败率 > 10%
    
  P2 - 一般:
    - 定义: 非核心功能异常
    - 响应时间: 1小时
    - 通知方式: 钉钉/邮件
    - 示例: 非核心接口延迟增加
    
  P3 - 提示:
    - 定义: 潜在问题
    - 响应时间: 工作时间
    - 通知方式: 邮件
    - 示例: 磁盘使用率 > 70%
```

---

## 附录

### A. 架构术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| BFF | Backend For Frontend | 前端专属后端 |
| SLA | Service Level Agreement | 服务等级协议 |
| SLO | Service Level Objective | 服务等级目标 |
| RTO | Recovery Time Objective | 恢复时间目标 |
| RPO | Recovery Point Objective | 恢复点目标 |
| HPA | Horizontal Pod Autoscaler | 水平 Pod 自动扩缩容 |
| VPC | Virtual Private Cloud | 虚拟私有云 |
| WAF | Web Application Firewall | Web应用防火墙 |
| DDoS | Distributed Denial of Service | 分布式拒绝服务 |

### B. 参考资料

- 《微服务架构设计模式》
- 《Designing Data-Intensive Applications》
- Kubernetes 官方文档
- Spring Cloud 官方文档

---

*文档结束*